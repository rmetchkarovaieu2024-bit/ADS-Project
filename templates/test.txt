<h2>Binary Search Tree Structure</h2>
                <p><strong>Algorithm:</strong> BST with recursive insertion based on points (lower left, higher right)</p>

                <div class="tree-stats">
                    <div class="stat-card">
                        <div class="value">{{ node_count }}</div>
                        <div class="label">Total Nodes</div>
                    </div>
                    <div class="stat-card">
                        <div class="value">{{ tree_height }}</div>
                        <div class="label">Tree Height</div>
                    </div>
                    <div class="stat-card">
                        <div class="value">O(log n)</div>
                        <div class="label">Insert Complexity</div>
                    </div>
                    <div class="stat-card">
                        <div class="value">O(n)</div>
                        <div class="label">Traversal Complexity</div>
                    </div>
                </div>
            </div>


            <!-- Controls -->
            <div class="controls">
                <h3>Test Controls</h3>
                <div class="button-group">
                    <button class="btn btn-primary" onclick="addPoints(10)">Add 10 Points</button>
                    <button class="btn btn-primary" onclick="addPoints(50)">Add 50 Points</button>
                    <button class="btn btn-primary" onclick="addPoints(100)">Add 100 Points</button>
                    <button class="btn btn-secondary" onclick="refreshLeaderboard()">Refresh Leaderboard</button>
                </div>
                <p style="margin-top: 10px; font-size: 13px; color: #666;">
                    Your current points: <strong>{{ user.points }}</strong>
                </p>
            </div>